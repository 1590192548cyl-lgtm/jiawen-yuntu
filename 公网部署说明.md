# 家稳云图公网部署说明

## 推荐方案：Cloudflare Pages + Worker

这个组合适合当前 MVP：前端静态资源部署简单，AI 密钥留在服务端，且不需要为 App 单独维护 iOS/Android 版本。

### 第一步：部署 AI Worker

在项目目录运行：

```bash
pnpm install
pnpm exec wrangler login
pnpm exec wrangler secret put MODEL_API_KEY
pnpm exec wrangler deploy
```

`MODEL_API_KEY` 会在终端中以隐藏输入方式写入 Cloudflare Secret，不要把它发到聊天、写入代码或提交到 GitHub。

部署完成后检查：

```bash
curl https://jiawen-ai.<你的账号>.workers.dev/health
```

### 第二步：部署静态前端

1. 在 Cloudflare 控制台打开 `Workers & Pages`。
2. 选择 `Create application` -> `Pages` -> 导入现有 GitHub 仓库。
3. Production branch 选择 `main`。
4. Build command 填写 `exit 0`。
5. Build output directory 填写 `.`。
6. 部署后会得到 `*.pages.dev` 域名。

### 第三步：收紧允许来源

前端域名确定后，在 Worker 的 Variables and Secrets 中添加普通变量：

```text
ALLOWED_ORIGINS=https://你的项目.pages.dev
```

如果后续增加正式域名，用英文逗号分隔：

```text
ALLOWED_ORIGINS=https://你的项目.pages.dev,https://你的正式域名
```

然后再次部署 Worker。此变量用于浏览器来源校验；Worker 中的限流用于降低公开接口被批量滥用的风险。

## 上线前检查

- 首页、建档、家庭概览、风险报告和 AI 顾问在手机与电脑端均能正常使用。
- `MODEL_API_KEY` 只存在于 Cloudflare Secret。
- `/health` 返回 `ok: true`。
- AI 顾问能正常回复，失败时会显示本地规则兜底建议。
- Worker 已启用每个匿名客户端每分钟 12 次请求限制。
- 正式开放前再接入 Turnstile，防止脚本批量消耗模型额度。

## 当前数据边界

- 问卷、评分和报告在浏览器本地运行，不保存到服务器。
- 主动使用 AI 顾问时，会把必要的家庭画像摘要发送至模型服务。
- 不要输入身份证号、银行卡号、账户密码或详细住址。
- 当前没有用户账户与云端档案；如需跨设备保存，再单独设计数据库与授权体系。

## App 是否更省钱

不会。App 仍需要公网 AI 后端和模型 API，同时还会增加应用商店、签名、审核和双端维护成本。当前响应式网站已经兼容手机与电脑，先用网页验证真实用户需求更合适；等用户留存和付费路径稳定后，再把现有网页封装为 PWA 或原生 App。
