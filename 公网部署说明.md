# 家稳云图公网部署说明

这是一个纯静态网页项目，只需要上传以下内容即可公网访问：

- `index.html`
- `styles.css`
- `app.js`
- `assets/logo.svg`

## 推荐方式：Netlify Drop

1. 打开 `https://app.netlify.com/drop`
2. 将本项目文件夹或 `家稳云图-web.zip` 拖进去。
3. 等待上传完成，Netlify 会自动生成一个公网链接。
4. 把生成的链接发给别人，对方即可直接访问。

## 也可以用 Vercel / Cloudflare Pages

上传时选择静态站点即可：

- Build command 留空
- Output directory 选择项目根目录
- 入口文件是 `index.html`

## 注意

当前网页是前端静态 MVP，不含后端数据库，也不会保存真实用户信息。
如果后续需要多人填写后保存数据，需要再接入后端服务或云数据库。

## 关于真实 AI 智能体

页面已预留开源模型接口配置，支持 Ollama 或 OpenAI-compatible 的开源模型服务。

示例：

- 接口地址：`http://localhost:11434/api/chat`
- 模型名称：`deepseek-r1:8b`

公网 GitHub Pages 不能直接安全托管 API Key。如果要给所有用户统一提供真实 AI，需要增加后端代理服务。

本项目已提供 `worker.js`，可复制到 Cloudflare Worker 中作为硅基流动 API 代理。
请在 Worker 环境变量中保存密钥：

- `MODEL_API_KEY`
- `MODEL_API_BASE`（可选，切换其他 OpenAI 兼容服务商时填写）
- `TAVILY_API_KEY`（可选，用于联网搜索）

不要把密钥写入前端或 GitHub。
